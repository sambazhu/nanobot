## 修正后的方案

保持 main 干净跟踪 upstream（不动它），网关改跑 `deploy` 集成分支——这正是它 8月15日 22:00 启动时用的代码，且 deploy 已包含 main（upstream/main@86d228b38）的全部提交和两个功能分支的完整内容，无需任何合并操作。

## 执行步骤

1. **切换工作区到 deploy**
   - `git checkout deploy`（当前 main 工作区干净，仅 .zcode/ 未跟踪目录，无影响；main 与 upstream/main 的跟踪关系保持不变）

2. **验证代码**
   - `pytest` 跑 dashscope 相关测试（tests/ 下 -k dashscope 或对应测试文件）
   - `ruff check nanobot/`

3. **重启网关**
   - 确认 launchd 服务（`launchctl list | grep -i nanobot`）
   - 优先用 `nanobot gateway restart`（CLI 已封装，必要时 `launchctl kickstart -k`）
   - 确认旧进程 5701 退出、新进程以 deploy 代码启动（中断仅数秒，KeepAlive 自动拉起）

4. **重启后检查**
   - `curl http://127.0.0.1:18790/health` 返回 `{"status": "ok"}`
   - launchd 日志新 banner 无报错、`dashscope_provider.py` 存在
   - 用 venv Python 对预设 `qwen3-8-max` 跑 `validate_provider_setup`，确认 provider 链解析正常
   - 你在微信上发一条测试消息，我 tail 日志确认回合成功

## 说明

- 本次不改配置、不改任何分支内容、不 push
- 今后 upstream 有新提交时按你现有流程：main 跟进 upstream → 把 main 合入 deploy/功能分支评估影响；只需注意**网关跑的是工作区当前分支，切分支后要重启网关**（本次故障的根因就是切了分支没重启）